name: Adding Bots Job
on: [push]
jobs:
  build:
    runs-on: windows-2019
    steps:
      - name: Display working directory
        run: |
          Get-Location
          dir
          echo $env:GITHAB_SHA